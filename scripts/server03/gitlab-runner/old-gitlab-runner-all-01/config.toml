log_level = "debug"
concurrent = 4
check_interval = 0
shutdown_timeout = 0

[session_server]
  session_timeout = 1800

[[runners]]
  name = "gitlab-runner-all-01"
  url = "http://192.168.201.83:63088/"
  token = "TsG4h-Dtsj"
  executor = "docker"
  output_limit = 10240
  [runners.custom_build_dir]
  [runners.cache]
    [runners.cache.s3]
    [runners.cache.gcs]
    [runners.cache.azure]
  [runners.docker]
    tls_verify = false
    image = "docker-group.repo.svc.internal/ubuntu:jammy-utils-latest"
    helper_image = "docker-group.repo.svc.internal/gitlab/gitlab-runner-helper:alpine3.21-x86_64-2c7703de"
    privileged = false
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    security_opt = ["seccomp=unconfined", "apparmor=unconfined"]
    volumes = ["/opt/docker_volumes/gitlab-runner-cache/old-gitlab-runner-all-01:/cache"]
    shm_size = 0
    pull_policy = ["always", "if-not-present"]
