#!/usr/sbin/nft -f

# /etc/nftables.conf

#flush ruleset

#table inet filter {
#       chain input {
#               type filter hook input priority 0;
#       }
#       chain forward {
#               type filter hook forward priority 0;
#       }
#       chain output {
#               type filter hook output priority 0;
#       }
#}

table ip appnat {
    chain prerouting {
        type nat hook prerouting priority -100;
        tcp dport 443 redirect to 22
    }
}